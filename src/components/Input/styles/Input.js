import styled from "styled-components/macro";
import { BaseInput } from "framework";
import Input from "util/input";
const InputWrapper = styled(BaseInput)`
  ${Input};
`;

export default InputWrapper;
